import { useEffect } from "react";

// Simplified: rely on static manifest (public/manifest.json) and main.tsx SW registration
export function PWAInstaller() {
  useEffect(() => {
    // no-op
  }, []);
  return null;
}

